services:
  mysql:
    image: mysql:8
    container_name: notification-platform-mysql

    ports:
      - '3307:3306'

    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: notification_platform

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: